import Tooltip from './theatrum-ui/tooltip';

// create tooltip
// const tooltip = new Tooltip(document.querySelector('.tooltip'));
// tooltip.init();

// CREATE TOOLTIP
const tooltip = new Tooltip(document.querySelector(".tooltip"));

tooltip.init();